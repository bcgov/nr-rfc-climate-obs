apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fireweather.fullname" . }}-test-connection"
  labels:
    {{- include "fireweather.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fireweather.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
